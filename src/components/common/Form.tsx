import React from 'react';

import { FormProps } from 'interfaces/common';

function Form({
  type,
  register,
  onFinish,
  formLoading,
  handleSubmit,
  handleImageChange,
  onFinishHandler,
  propertyImage,
}: FormProps) {
  return <div>Form</div>;
}

export default Form;
